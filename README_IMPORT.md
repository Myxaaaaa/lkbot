# 📥 Импорт данных в бота

## ✅ Готово!

Все данные из Google Таблицы успешно импортированы в бота!

**Импортировано:** 240 новых записей  
**Всего записей в базе:** 243

## 📋 Как использовать скрипт импорта

### Простой способ (из текстового файла):

1. Поместите данные в файл `import_data.txt` в формате:
   ```
   ФИО	Телефон	Карта	Статус
   ```

2. Запустите:
   ```bash
   python import_from_google.py
   ```

### Импорт из Google Sheets:

1. Установите библиотеки:
   ```bash
   pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
   ```

2. Настройте Google API (см. `IMPORT_INSTRUCTIONS.md`)

3. Используйте функцию `import_from_google_sheets()` в скрипте

## 🔄 Повторный импорт

Скрипт автоматически:
- ✅ Проверяет дубликаты по телефону
- ✅ Объединяет с существующими записями
- ✅ Генерирует новые ID
- ✅ Нормализует телефоны (добавляет +996)
- ✅ Определяет банк автоматически
- ✅ Маппит статусы из таблицы в статусы бота

## 📊 Статусы

Автоматический маппинг:
- `у дропа` → `в работе`
- `отдых` → `на отдыхе`
- `блок` → `заблокирован`
- `вылет` → `Вылет`
- `актив` → `в работе`
- `ждем карту` → `NEW-white`
- `Need to white` → `NEW-white`
- и другие...

## 🚀 Запуск бота

После импорта просто запустите бота:
```bash
python lk_registry_bot.py
```

Все импортированные данные будут доступны в боте!




