# 📥 Инструкция по импорту данных из Google Таблиц

## Быстрый старт

### Вариант 1: Импорт из текстового файла (простой способ)

1. Поместите данные в файл `import_data.txt` в формате:
   ```
   ФИО	Телефон	Карта	Статус
   ```

2. Запустите скрипт:
   ```bash
   python import_from_google.py
   ```

3. Данные автоматически импортируются в `lk_registry.json`

### Вариант 2: Импорт из Google Sheets API

1. Установите необходимые библиотеки:
   ```bash
   pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
   ```

2. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)

3. Включите Google Sheets API

4. Создайте Service Account и скачайте `credentials.json`

5. Поделитесь Google Таблицей с email из `credentials.json`

6. Используйте функцию в скрипте:
   ```python
   from import_from_google import import_from_google_sheets
   
   sheet_id = "YOUR_SHEET_ID"  # Из URL таблицы
   imported = import_from_google_sheets(sheet_id, "A:D", "credentials.json")
   ```

## Формат данных

Данные должны быть в формате:
- **Колонка 1**: ФИО
- **Колонка 2**: Телефон (можно без +996)
- **Колонка 3**: Номер карты (опционально)
- **Колонка 4**: Статус

## Маппинг статусов

Скрипт автоматически преобразует статусы из таблицы в статусы бота:

| Статус в таблице | Статус в боте |
|-----------------|---------------|
| у дропа | в работе |
| отдых | на отдыхе |
| блок | заблокирован |
| вылет | Вылет |
| актив | в работе |
| виртуалка | в работе |
| ждем карту | NEW-white |
| Need to white | NEW-white |
| и другие... | в работе (по умолчанию) |

## Особенности

- ✅ Автоматическое определение банка по имени или карте
- ✅ Нормализация номеров телефонов (добавление +996)
- ✅ Проверка на дубликаты по телефону
- ✅ Автоматическая генерация ID
- ✅ Сохранение остатка средств для статусов "Вылет" и "заблокирован"

## Пример использования

```python
from import_from_google import import_from_text

data = """
Иванов Иван Иванович	755 123 456	4177 4901 1234 5678	отдых
Петров Петр Петрович	703 987 654	4177 4901 8765 4321	блок
"""

imported = import_from_text(data, merge=True)
print(f"Импортировано: {imported} записей")
```

