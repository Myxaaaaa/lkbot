# 🚀 Деплой бота на Railway

## Быстрый старт

### 1. Подготовка репозитория

1. Убедитесь, что все файлы закоммичены:
   ```bash
   git add .
   git commit -m "Prepare for Railway deployment"
   ```

2. Создайте репозиторий на GitHub (если еще нет):
   ```bash
   git remote add origin https://github.com/yourusername/lkbot.git
   git push -u origin main
   ```

### 2. Деплой на Railway

1. Перейдите на [Railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `lkbot`
6. Railway автоматически определит проект как Python

### 3. Настройка переменных окружения

1. В проекте Railway откройте вкладку **"Variables"**
2. Добавьте переменную:
   - **Name:** `BOT_TOKEN`
   - **Value:** ваш токен бота (получите у @BotFather в Telegram)

### 4. Запуск

Railway автоматически:
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит бота через `Procfile`
- ✅ Будет перезапускать бота при сбоях

### 5. Проверка работы

1. Откройте вкладку **"Deployments"** в Railway
2. Проверьте логи - должны быть сообщения о запуске бота
3. Напишите боту в Telegram команду `/start`

## 📋 Структура файлов для деплоя

- `Procfile` - команда запуска для Railway
- `requirements.txt` - зависимости Python
- `runtime.txt` - версия Python (опционально)
- `railway.json` - конфигурация Railway (опционально)
- `.env.example` - пример переменных окружения

## 🔧 Настройки

### Изменение версии Python

Отредактируйте `runtime.txt`:
```
python-3.11.0
```

### Добавление других переменных окружения

В Railway → Variables добавьте нужные переменные.

## 📝 Важные замечания

1. **Токен бота:** Никогда не коммитьте токен в репозиторий! Используйте переменные окружения.

2. **Файл данных:** `lk_registry.json` будет создаваться автоматически на сервере. Для переноса данных:
   - Экспортируйте данные из локального файла
   - Импортируйте через бота или скрипт

3. **Логи:** Все логи доступны во вкладке **"Deployments"** → **"View Logs"**

4. **Перезапуск:** Railway автоматически перезапускает бота при:
   - Обновлении кода (новый деплой)
   - Сбоях в работе
   - Перезапуске сервиса

## 🆘 Решение проблем

### Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что переменная `BOT_TOKEN` установлена
- Проверьте, что бот запущен (статус "Running")

### Ошибки при деплое
- Проверьте `requirements.txt` - все зависимости указаны?
- Убедитесь, что версия Python поддерживается
- Проверьте логи деплоя

### Данные не сохраняются
- Railway использует эфемерную файловую систему
- Для постоянного хранения рассмотрите использование базы данных (PostgreSQL, MongoDB)

## 🔄 Обновление бота

1. Внесите изменения в код
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```
3. Railway автоматически задеплоит новую версию

## 💰 Стоимость

Railway предоставляет бесплатный план с ограничениями:
- $5 бесплатных кредитов в месяц
- Этого достаточно для небольшого бота

При превышении лимита потребуется платный план.

